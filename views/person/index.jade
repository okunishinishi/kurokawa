extends ../layout

block stylesheets
    link(rel='stylesheet', href=url.css('person.css'))

block javascripts
    script(src=url.js('p.person.js'))

block left-content
    if person.company_name
        span=person.company_name
    h1.page-title=person.name

